# VRM Models

Place a Blender-exported `.vrm`, `.glb`, or `.gltf` file in this folder.

The VRM renderer loads the first matching file it finds here. If the folder is
empty, the app shows a simple built-in 3D placeholder so the renderer can still
be tested.
